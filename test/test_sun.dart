import 'package:flutter_test/flutter_test.dart'; import 'package:astroplan/domain/services/visibility_calculator.dart'; void main() { test('Sun Alt', () { final date = DateTime.utc(2026, 9, 18, 12, 0); final lat = 51.5072; final lon = -0.1276; for (int i = 0; i <= 24*60; i += 60) { final current = date.add(Duration(minutes: i)); final alt = VisibilityCalculator.calculateSunAltitude(current, lat, lon); print(' -> Alt: '); } }); }
